# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout somewhen, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see http://i3wm.org/docs/userguide.html for a complete reference!

set $mod Mod4
set $dmenu "dmenu -b -f -i -l 10 -fn '-*-Verdana-*-*-*-*-25-*-*-*-*-*-*-*'"

# font for window titles. ISO 10646 = Unicode
#set $fonttype "-misc-fixed-medium-r-normal--13-120-75-75-C-70-iso10646-1"
#set $fonttype "pango:Terminus 10"
set $fonttype "pango:i3barsymbols 8"
font $fonttype

set $term kitty
# GET DEM BORDERS OUT OF HERE
# Thanks Childz
for_window [class="^.*"] border pixel 2
                          # class                 border  backgr. text    indicator child_border
for_window [class="^.*"] client.focused          #77dd77 #285577 #ffffff #2e9ef4   #285577

# Numlock on at start
input type:keyboard xkb_numlock enabled

# workspaces
set $WS1 1
set $WS2 2
set $WS3 3
set $WS4 4
set $WS5 5
set $WS6 6
set $WS7 7
set $WS8 8
set $WS9 9
set $WS10 10

# workspace on big monitor
workspace 1 output DP-2
workspace 2 output DP-2
workspace 3 output DP-2
workspace 4 output DP-2
workspace 5 output DP-2
workspace 7 output DP-2
workspace 8 output DP-2
workspace 9 output DP-2

# workspace on laptop
workspace 6 output eDP-1
workspace 10 output eDP-1
# assigns
assign [class="Discord"] → $WS6

# floating by default
## Firefox
for_window [title="Library"] floating enable
for_window [title="Firefox Preferences"] floating enable

# Bind applications
## launch firefox
bindsym $mod+v exec --no-startup-id firefox

## launch kitty (terminal)
bindsym $mod+Return exec kitty

# Navigation
## Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

## enable workspace toggle
workspace_auto_back_and_forth yes

## use d to switch toggle
bindsym $mod+w workspace back_and_forth

## use Shift d to move a container back and forth and follow it
bindsym $mod+Shift+w move container to workspace back_and_forth; workspace back_and_forth

## browse between containers as if they were buffers in  vimperator.
## see https://github.com/proxypoke/quickswitch-for-i3
#bindsym $mod+b exec --no-startup-id quickswitch.py --dmenu=$dmenu

## browse between containers as if they were buffers in  vimperator and make it move to the current workspace.
## see https://github.com/proxypoke/quickswitch-for-i3
#bindsym $mod+Shift+b exec --no-startup-id quickswitch.py -m --dmenu=$dmenu

## kill focused window
bindsym $mod+z kill

## start dmenu (a program launcher)
#bindsym $mod+d exec --no-startup-id i3-dmenu-desktop
#bindsym $mod+d exec --no-startup-id rofi -show drun
bindsym $mod+d exec --no-startup-id sirula

## change focus
bindsym $mod+p focus left
bindsym $mod+h focus down
bindsym $mod+a focus up
bindsym $mod+e focus right

## move focused window
bindsym $mod+Shift+p move left
bindsym $mod+Shift+h move down
bindsym $mod+Shift+a move up
bindsym $mod+Shift+e move right

# change systeme volume
bindsym $mod+apostrophe exec --no-startup-id amixer set Master 5%+ unmute -q
bindsym $mod+f exec --no-startup-id amixer set Master 5%- unmute -q
#bindsym $mod+bracketright exec --no-startup-id amixer set Master toggle -q

# clementine Remote control
#bindsym XF86AudioPlay exec clementine_CTRL.py pause
#bindsym Shift+XF86AudioPlay exec clementine_CTRL.py stop
#bindsym Control+XF86AudioPlay exec clementine_CTRL.py osd
#bindsym Control+XF86AudioRaiseVolume exec clementine_CTRL.py next
#bindsym Control+XF86AudioLowerVolume exec clementine_CTRL.py previous

## change system brightness
# bindsym $mod+o exec --no-startup-id bash .local/bin/brightness_level.sh +
# bindsym $mod+y exec --no-startup-id bash .local/bin/brightness_level.sh -
 bindsym $mod+o exec --no-startup-id bash .local/bin/brightness_level_wayland.sh +
 bindsym $mod+y exec --no-startup-id bash .local/bin/brightness_level_wayland.sh -
# bindsym $mod+y exec `brightnessctl -d ddcci18 s 5%- && brightnessctl -d nv_backlight s 5%-`
# bindsym $mod+o exec `brightnessctl -d ddcci18 s 5%+ && brightnessctl -d nv_backlight s 5%+`
# bindsym $mod+y exec `brillo -el -U 3`
# bindsym $mod+o exec `brillo -el -A 3`

# split in horizontal orientation
bindsym $mod+shift+f split h

# split in vertical orientation
bindsym $mod+shift+k split v

# enter fullscreen mode for the focused container
bindsym $mod+c fullscreen

# change container layout (stacked, tabbed, default)
bindsym $mod+q layout stacking
bindsym $mod+b layout tabbed
bindsym $mod+l layout toggle split

# toggle tiling / floating
bindsym $mod+Shift+BackSpace floating toggle

# change focus between tiling / floating windows
bindsym $mod+BackSpace focus mode_toggle

# focus the parent container
bindsym $mod+u focus parent

# focus the child container
#bindcode $mod+j focus child

# jump to last urgent window
#bindsym $mod+q [urgent=latest] focus

# switch to workspace
bindsym  $mod+n  workspace  $WS1
bindsym  $mod+r  workspace  $WS2
bindsym  $mod+t  workspace  $WS3
bindsym  $mod+s  workspace  $WS4
bindsym  $mod+g  workspace  $WS5
bindsym  $mod+F1  workspace  $WS6
bindsym  $mod+F2  workspace  $WS7
bindsym  $mod+F3  workspace  $WS8
bindsym  $mod+F4  workspace  $WS9
bindsym  $mod+F5  workspace  $WS10

# move focused container to workspace
bindsym  $mod+Shift+n  move  container  to  workspace  $WS1
bindsym  $mod+Shift+r  move  container  to  workspace  $WS2
bindsym  $mod+Shift+t  move  container  to  workspace  $WS3
bindsym  $mod+Shift+s  move  container  to  workspace  $WS4
bindsym  $mod+Shift+g  move  container  to  workspace  $WS5
bindsym  $mod+Shift+F1  move  container  to  workspace  $WS6
bindsym  $mod+Shift+F2  move  container  to  workspace  $WS7
bindsym  $mod+Shift+F3  move  container  to  workspace  $WS8
bindsym  $mod+Shift+F4  move  container  to  workspace  $WS9
bindsym  $mod+Shift+F5  move  container  to  workspace  $WS10




# reload the configuration file
#bindsym $mod+f reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+x reload
# exit i3 (logs # exit i3 (logs you out of your X session) but ask a confirmation before
bindsym $mod+Shift+j exec "swaynag -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -b 'Yes, exit i3' 'swaymsg exit'"

# resize window (you can also use the mouse for that)
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        bindsym e resize shrink width 10 px or 10 ppt
        bindsym a resize grow height 10 px or 10 ppt
        bindsym h resize shrink height 10 px or 10 ppt
        bindsym p resize grow width 10 px or 10 ppt

        # back to normal: Enter or Escape
        bindsym Return mode "default"
        bindsym Escape mode "default"
}

bindsym $mod+shift+colon mode "resize"

# Vim-like/emacs-like marks
## This is awesome, see : http://www.slackword.net/?p=657
mode "focused" {

                # hardcoded focus keybindings
                bindsym i [class="(?i)firefox"] focus
                #bindsym e [class="(?i)thunderbird"] focus
                bindsym e [title="(?i)mutt"] focus
                bindsym u [class="(?i)zathura"] focus

                # keybindings for marking and jumping to clients
                bindsym a exec i3-input -F 'mark %s' -P 'Mark name: '
                bindsym comma exec i3-input -F '[con_mark=%s] focus' -P 'Go to mark: '

                # Assign marks to keys 1-5
                bindsym Shift+multiply mark mark1
                bindsym Shift+w mark mark2
                bindsym Shift+quotedbl mark mark3
                bindsym Shift+apostrophe mark mark4
                bindsym Shift+grave mark mark5

                # Jump to clients marked 1-5
                bindsym multiply [con_mark="mark1"] focus
                bindsym w [con_mark="mark2"] focus
                bindsym quotedbl [con_mark="mark3"] focus
                bindsym apostrophe [con_mark="mark4"] focus
                bindsym grave [con_mark="mark5"] focus

                # Exit to the default mode
                bindsym Return mode "default"
                bindsym Escape mode "default"
}

#bindsym $mod+s mode "focused"



# Start i3bar to display a workspace bar (plus the system information i3status finds out, if available)
bar {
	font pango:i3barsymbols 17.5
 	status_command $HOME/.config/i3/conky/conky-i3bar
}



# Exec on startup
## take care of automounting
exec --no-startup-id "devmon --no-gui"

## Background
output * bg $HOME/.config/sway/wallpaper/gawa.jpg fill

## hide mouse cursor when not used
exec_always --no-startup-id unclutter -idle 1 -jitter 2 -root &

## Keypress repeat rate
input * repeat_delay 250
input * repeat_rate 25

output DP-2 mode 2560x1440@119.998Hz position 1919,0
output eDP-1 mode 1920x1080@59.977Hz position 0,0

## Terminal
exec --no-startup-id swaymsg 'workspace $WS3; exec kitty'

exec --no-startup-id 'sudo modprobe ddcci'

include @sysconfdir@/sway/config.d/*

exec wl-paste -t text --watch clipman store --no-persist

exec 'dbus-update-activation-environment --systemd DISPLAY WAYLAND_DISPLAY SWAYSOCK'
