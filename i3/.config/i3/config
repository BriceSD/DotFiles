# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout somewhen, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see http://i3wm.org/docs/userguide.html for a complete reference!

set $mod Mod4
set $dmenu "dmenu -b -f -i -l 10 -fn '-*-Verdana-*-*-*-*-25-*-*-*-*-*-*-*'"

# font for window titles. ISO 10646 = Unicode
#set $fonttype "-misc-fixed-medium-r-normal--13-120-75-75-C-70-iso10646-1"
#set $fonttype "pango:Terminus 10"
set $fonttype "pango:i3barsymbols 8"
font $fonttype

# GET DEM BORDERS OUT OF HERE
# Thanks Childz
for_window [class="^.*"] border pixel 2
                          # class                 border  backgr. text    indicator child_border
for_window [class="^.*"] client.focused          #77dd77 #285577 #ffffff #2e9ef4   #285577

# workspaces
set $WS1 1
set $WS2 2
set $WS3 3
set $WS4 4
set $WS5 5
set $WS6 6
set $WS7 7
set $WS8 8
set $WS9 9
set $WS10 10

# workspace on big monitor
workspace 1 output DP-2
workspace 2 output DP-2
workspace 3 output DP-2
workspace 4 output DP-2
workspace 5 output DP-2
workspace 7 output DP-2
workspace 8 output DP-2
workspace 9 output DP-2

# workspace on laptop
workspace 6 output DP-4
workspace 10 output DP-4

# assigns
assign [class="Discord"] → $WS6

# floating by default
## Firefox
for_window [title="Library"] floating enable
for_window [title="Firefox Preferences"] floating enable

# Bind applications
## launch firefox
bindsym $mod+y exec --no-startup-id firefox

## launch intellij idea
#bindsym $mod+y exec --no-startup-id intellij-idea-ultimate-edition
#
## launch pycharm
#bindsym $mod+agrave exec --no-startup-id sh $HOME/Pycharm/bin/pycharm.sh

## launch urxvt (terminal)
#bindsym $mod+Return exec urxvt
bindsym $mod+Return exec terminology

# Navigation
## Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

## enable workspace toggle
workspace_auto_back_and_forth yes

## use d to switch toggle
bindsym $mod+g workspace back_and_forth

## use Shift d to move a container back and forth and follow it
bindsym $mod+Shift+g move container to workspace back_and_forth; workspace back_and_forth

## browse between containers as if they were buffers in  vimperator.
## see https://github.com/proxypoke/quickswitch-for-i3
#bindsym $mod+b exec --no-startup-id quickswitch.py --dmenu=$dmenu

## browse between containers as if they were buffers in  vimperator and make it move to the current workspace.
## see https://github.com/proxypoke/quickswitch-for-i3
#bindsym $mod+Shift+b exec --no-startup-id quickswitch.py -m --dmenu=$dmenu

## kill focused window
bindsym $mod+z kill

## start dmenu (a program launcher)
bindsym $mod+r exec --no-startup-id i3-dmenu-desktop

## change focus
bindsym $mod+slash focus left
bindsym $mod+n focus down
bindsym $mod+a focus up
bindsym $mod+e focus right

## move focused window
bindsym $mod+Shift+slash move left
bindsym $mod+Shift+n move down
bindsym $mod+Shift+a move up
bindsym $mod+Shift+e move right

# change systeme volume
bindcode 123 exec --no-startup-id amixer set Master 5%+ unmute -q
bindcode 122 exec --no-startup-id amixer set Master 5%- unmute -q
bindcode 121 exec --no-startup-id amixer set Master toggle -q

# clementine Remote control
#bindsym XF86AudioPlay exec clementine_CTRL.py pause
#bindsym Shift+XF86AudioPlay exec clementine_CTRL.py stop
#bindsym Control+XF86AudioPlay exec clementine_CTRL.py osd
#bindsym Control+XF86AudioRaiseVolume exec clementine_CTRL.py next
#bindsym Control+XF86AudioLowerVolume exec clementine_CTRL.py previous

## change system brightness
bindsym $mod+Shift+less exec --no-startup-id bash .local/scripts/brightness_level.sh +
bindsym $mod+Shift+braceleft exec --no-startup-id bash .local/scripts/brightness_level.sh -

# split in horizontal orientation
bindsym $mod+b split h

# split in vertical orientation
bindsym $mod+h split v

# enter fullscreen mode for the focused container
bindsym $mod+d fullscreen

# change container layout (stacked, tabbed, default)
bindsym $mod+k layout stacking
bindsym $mod+t layout tabbed
bindsym $mod+s layout default

# toggle tiling / floating
bindsym $mod+Shift+BackSpace floating toggle

# change focus between tiling / floating windows
bindsym $mod+BackSpace focus mode_toggle

# focus the parent container
bindsym $mod+u focus parent

# focus the child container
bindcode $mod+j focus child

# jump to last urgent window
#bindsym $mod+q [urgent=latest] focus

# switch to workspace
bindsym $mod+v workspace   $WS1
bindsym $mod+m workspace          $WS2
bindsym $mod+l workspace   $WS3
bindsym $mod+c workspace $WS4
bindsym $mod+p workspace      $WS5
bindsym $mod+1 workspace $WS6
bindsym $mod+7 workspace   $WS7
bindsym $mod+3 workspace      $WS8
bindsym $mod+9 workspace  $WS9
bindsym $mod+5 workspace $WS10

# move focused container to workspace
bindsym $mod+Shift+v move container to workspace    $WS1
bindsym $mod+Shift+m move container to workspace           $WS2
bindsym $mod+Shift+l move container to workspace    $WS3
bindsym $mod+Shift+c move container to workspace  $WS4
bindsym $mod+Shift+p move container to workspace       $WS5
bindsym $mod+Shift+1 move container to workspace  $WS6
bindsym $mod+Shift+7 move container to workspace    $WS7
bindsym $mod+Shift+3 move container to workspace       $WS8
bindsym $mod+Shift+9 move container to workspace   $WS9
bindsym $mod+Shift+5 move container to workspace  $WS10




# reload the configuration file
bindsym $mod+f reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+x restart
# exit i3 (logs # exit i3 (logs you out of your X session) but ask a confirmation before
bindsym $mod+Shift+j exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -b 'Yes, exit i3' 'i3-msg exit'"

# resize window (you can also use the mouse for that)
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        bindsym e resize shrink width 10 px or 10 ppt
        bindsym a resize grow height 10 px or 10 ppt
        bindsym n resize shrink height 10 px or 10 ppt
        bindsym slash resize grow width 10 px or 10 ppt

        # back to normal: Enter or Escape
        bindsym Return mode "default"
        bindsym Escape mode "default"
}

bindsym $mod+apostrophe mode "resize"

# Vim-like/emacs-like marks
## This is awesome, see : http://www.slackword.net/?p=657
mode "focused" {

                # hardcoded focus keybindings
                bindsym i [class="(?i)firefox"] focus
                #bindsym e [class="(?i)thunderbird"] focus
                bindsym e [title="(?i)mutt"] focus
                bindsym u [class="(?i)zathura"] focus

                # keybindings for marking and jumping to clients
                bindsym a exec i3-input -F 'mark %s' -P 'Mark name: '
                bindsym comma exec i3-input -F '[con_mark=%s] focus' -P 'Go to mark: '

                # Assign marks to keys 1-5
                bindsym Shift+multiply mark mark1
                bindsym Shift+w mark mark2
                bindsym Shift+quotedbl mark mark3
                bindsym Shift+apostrophe mark mark4
                bindsym Shift+grave mark mark5

                # Jump to clients marked 1-5
                bindsym multiply [con_mark="mark1"] focus
                bindsym w [con_mark="mark2"] focus
                bindsym quotedbl [con_mark="mark3"] focus
                bindsym apostrophe [con_mark="mark4"] focus
                bindsym grave [con_mark="mark5"] focus

                # Exit to the default mode
                bindsym Return mode "default"
                bindsym Escape mode "default"
}

#bindsym $mod+s mode "focused"



# Start i3bar to display a workspace bar (plus the system information i3status finds out, if available)
bar {
	font pango:i3barsymbols 10.5
 	status_command $HOME/.config/i3/conky/conky-i3bar
}



# Exec on startup
## take care of automounting
exec --no-startup-id "devmon --no-gui"

## Background
exec --no-startup-id feh --bg-scale $HOME/.config/i3/wallpaper/Stained-Glass-Vector-Lines.jpg

## hide mouse cursor when not used
exec_always --no-startup-id unclutter -idle 1 -jitter 2 -root &

## Keypress repeat rate
exec_always --no-startup-id  "sh `xset r rate 200 35`"

exec --no-startup-id i3-msg 'workspace $WS6; exec discord'

## Music
#exec --no-startup-id i3-msg 'workspace $WS8; exec clementine'

## JDownloader
#exec --no-startup-id i3-msg 'workspace $WS10; exec /bin/JDownloader'

## Terminal
#exec --no-startup-id i3-msg 'workspace $WS3; exec urxvt'
exec --no-startup-id i3-msg 'workspace $WS3; exec terminology'

